# ---------------------------------------------------------------
# PragmaLogixAI — Python Dependencies
# ---------------------------------------------------------------

# Web framework & server
fastapi>=0.110.0
uvicorn>=0.28.0
python-multipart>=0.0.9        # multipart/form-data file uploads
aiofiles>=23.2.1               # async file I/O for GCS staging

# Data validation
pydantic>=2.6.0
pydantic-settings>=2.2.0       # settings management from env/Secret Manager

# Database — async PostgreSQL + pgvector
sqlalchemy>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0             # SQLite fallback for unit tests (no PostgreSQL required in CI)
pgvector>=0.2.5

# Google Cloud & Vertex AI (no raw API key — uses ADC / service accounts)
google-cloud-aiplatform>=1.60.0    # includes vertexai SDK for Gemini models
google-cloud-storage>=2.16.0       # GCS for multimodal file staging
google-cloud-bigquery>=3.20.0      # BigQuery analytics writes
google-cloud-secret-manager>=2.20.0  # Secret Manager for DB credentials
google-cloud-texttospeech>=2.16.0    # Cloud TTS (Chirp HD / Journey voice)

# HTTP client (async — used by FastAPI test client & internal calls)
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
